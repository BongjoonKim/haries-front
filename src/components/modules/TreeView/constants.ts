export const enum SelectionFormat {
    DEFAULT = "Default",
    ALL = "All"
}

export const enum UniqueIdScope {
    ALL = "All",
    DEPTH = "Depth",
    UNKNOWN = "Unknown"
}

export const enum InitialSelection {
    COMPARE = "Compare",
    FIRST = "First",
    NONE = "None"
}

export const enum ItemKeys {
    id = "id",
    label = "label",
    name = "name",
    type = "type",
    options = "options",
    children = "children"
}